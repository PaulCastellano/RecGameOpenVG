#include "../include/include.h"

void    Game::setWindowWidth(int windowWidth)
{
    this->windowWidth = windowWidth;
}
void    Game::setWindowHeight(int windowHeight)
{
    this->windowHeight = windowHeight;
}
void    Game::setScore(int score)
{
    this->score = score;
}
void    Game::setMode(int mode)
{
    this->mode = mode;
}
void    Game::setZone(int zone)
{
    this->zone = zone;
}
void    Game::setColorMode(int colorMode)
{
    this->colorMode = colorMode;
}
void    Game::setTime(int time)
{
    this->time = time;
}
void    Game::setTimerCount(int timerCount)
{
    this->timerCount = timerCount;
}